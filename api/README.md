# API Structure
```
api/ - root folder
api/models - Models and schemas (per file)
api/endpoints - all endpoints (one per file)
api/middlewares - all middleware (one per file)
api/includes - all side scripts
__init__.py - 
database.py - config with db
main.py - core file
run.ps1
```